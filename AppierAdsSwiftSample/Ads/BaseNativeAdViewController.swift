import UIKit

class BaseNativeAdViewController: UIViewController {
    @IBOutlet weak var nativeAdPlaceholder: UIView!

    override func viewDidLoad() {
        super.viewDidLoad()

        // Do any additional setup after loading the view.
        view.backgroundColor = .white
    }
}
