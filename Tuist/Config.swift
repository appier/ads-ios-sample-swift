import ProjectDescription

print()
print("Generate AppierAdsSample.xcworkspace...")

let config = Config(
    plugins: [.local(path: .relativeToRoot("./"))],
    generationOptions: [
        .disableAutogeneratedSchemes,
        .disableSynthesizedResourceAccessors
    ]
)
